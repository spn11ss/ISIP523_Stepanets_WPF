<Page x:Class="WpfApp1.Page1_Product"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Каталог товаров">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" Padding="10" Margin="0,0,0,10">
            <TextBlock Text="Каталог парфюмерии" 
                       FontSize="20" FontFamily="Dubai" FontWeight="Bold" 
                       Foreground="#000080" HorizontalAlignment="Center"/>
        </Border>

        <ListBox Grid.Row="1" x:Name="Page1LstBx" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Background="#e0ffff">
            <ListBox.ItemTemplate>
                <ItemContainerTemplate>
                    <StackPanel Height="350" Width="250">
                        <Image   Height="200" Width="200"  Margin="0,0,10,0"  Source="{Binding Picture}"/>
                        <TextBlock FontSize="16" FontFamily="Dubai" FontWeight="Bold" 
                                   Foreground="#000080" Text="{Binding Name}"/>
                        <TextBlock Margin="0,5,0,10" FontSize="14" FontFamily="Dubai" Foreground="Green" Text="{Binding Price, StringFormat='Цена: {0} руб.'}"/>
                        <Button    Margin="5"  Content="В корзину" Click="Vkorzinu_Click"/>
                    </StackPanel>
                </ItemContainerTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
    </Grid>
</Page>

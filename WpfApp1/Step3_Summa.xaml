<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="WpfApp1.Step3_Summa"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="Шаг3_Summary"
      d:DesignHeight="450" d:DesignWidth="800"
      Loaded="Page_Loaded">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="#DB4D92" CornerRadius="5" Padding="15" Margin="0,0,0,20">
        <TextBlock Grid.Row="0" Text="Шаг 3: Итоговая стоимость"
                   FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
        </Border>
        <StackPanel Grid.Row="1" Margin="10">
            <TextBlock Text="Выбранная конфигурация:" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

            <StackPanel Margin="0,0,0,20">

                <TextBlock x:Name="ModelText"/>
                <TextBlock x:Name="EngineText"/>
                <TextBlock x:Name="ColorText"/>
                <TextBlock x:Name="OptionsText"/>
            </StackPanel>

            <TextBlock Text="Составляющие стоимости:" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

            <StackPanel>
                <TextBlock x:Name="BasePriceText"/>
                <TextBlock x:Name="EnginePriceText"/>
                <TextBlock x:Name="ColorPriceText"/>
                <TextBlock x:Name="OptionsPriceText"/>
            </StackPanel>

            <Separator Margin="0,10"/>

            <TextBlock x:Name="TotalPriceText" FontSize="18" FontWeight="Bold" Margin="0,10"/>
        </StackPanel>
    </Grid>
</Page>
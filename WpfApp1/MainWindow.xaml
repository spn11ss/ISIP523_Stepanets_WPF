<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Конфигуратор автомобиля" 
        Background="#FFB6BB"
        MinHeight="600" MinWidth="800"
        Height="650" Width="900"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="36"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>


        <TextBlock Grid.Row="0" Text="Конфигуратор автомобиля" 
                   FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,15,0,10"/>


        <Grid Grid.Row="1" Margin="20,0,20,10">
            <ProgressBar Minimum="1" Maximum="5" 
                         x:Name="ProgressBar" Height="25"/>
            <TextBlock Text="{Binding ElementName=ProgressBar, Path=Value, StringFormat=Шаг {0} из 5}"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       FontWeight="Bold"/>
        </Grid>


        <Frame Grid.Row="2" x:Name="MainFrame" NavigationUIVisibility="Hidden"
               Navigated="MainFrame_OnNavigated" Margin="10,10,10,10"/>


        <Border Grid.Row="2" Background="#FFB6C1" BorderThickness="0,1,0,0" Margin="0,466,0,0" Grid.RowSpan="3">
            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Center" 
                        VerticalAlignment="Center"
                        Margin="10" Height="55">
                        

                <Button x:Name="BackButton" Content="Назад" 
                        Click="BackButton_OnClick" 
                        Margin="10,0" 
                        Width="200" Height="40" FontSize="14"/>

                <Button x:Name="NextButton" Content="Далее" 
                        Click="NextButton_OnClick"
                        Margin="10,0"
                        Width="200" Height="40" FontSize="14"/>

            </StackPanel>
        </Border>
    </Grid>
</Window>